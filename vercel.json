{
  "buildCommand": "next build",
  "builds": [
    { "src": "package.json", "use": "@vercel/next" },
    { "src": "app/api/main.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "app/api/main.py" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}